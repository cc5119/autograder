[package]
name = "harness"
version = "0.0.0"
edition = "2024"

[features]
student = ["{id}/student"]

[[bin]]
name = "driver"
path = "src/bin/driver.rs"
test = false

[dependencies]
{id} = { path = "../{id}" }

[dev-dependencies]
autograder-test = { git = "https://github.com/nilehmann/autograder" }
