[assignment]
id = "{id}"
kind = "binary"
deadline = "{deadline}"
harness = "harness"

[sandbox]
image = "autograder-base:1.86.0"

[build-limits]
wall-clock = "180s"
cpus = 2
memory = "2GiB"
pids = 256
max-output-bytes = "2MiB"

[scoring]
formula = "sum"
base = 0.0
