[assignment]
id = "{id}"
kind = "binary"
deadline = "{deadline}"
harness = "harness"

[sandbox]
image = "autograder-base:1.86.0"

[build-limits]
wall-clock = "60s"
cpus = 2
memory = "1GiB"
pids = 128
max-output-bytes = "64KiB"

[scoring]
formula = "sum"
base = 0.0
